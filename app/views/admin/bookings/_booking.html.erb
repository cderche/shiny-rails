<div class="panel panel-default">
  <div class="panel-body">
    <div class="row">
      <div class="col-sm-12">
        <%= render 'form' %>
      </div>
    </div>
    <div class="btn-group">
      <%= link_to 'Edit', edit_admin_booking_path(@booking), class: 'btn btn-primary' if @readonly %>
      <%= link_to 'Invoice', new_admin_invoice_path(booking_id: @booking), class: 'btn btn-complete' if @readonly %>
    </div>
  </div>
</div>
