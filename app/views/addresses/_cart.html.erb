<div class="hide-wrapper hide">
  <input type="radio" name="cart[frequency]" value="<%=@cart.frequency%>" data-name="<%= t(@cart.frequency) %>" checked="checked">
  <input type="hidden" name="cart[date]" value="<%= @cart.date %>" >
  <input type="hidden" name="cart[time]" value="<%= @cart.time %>" >
  <input type="hidden" name="cart[duration]" value="<%= @cart.duration %>" >
  <input type="hidden" name="cart[cost]" value="<%= @cart.cost %>" >
  <input type="hidden" name="cart[disc]" value="<%= @cart.disc %>" >
  <input type="hidden" name="cart[real]" value="<%= @cart.real %>" >
</div>
